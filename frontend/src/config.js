const config = {
  backend: "http://localhost:4500",
  mongodb: "localhost:27017"
};
// const config = {
//   backend: "http://localhost:4500",
//   mongodb: "localhost:27017"
// };

//not yet connected

export default config;
